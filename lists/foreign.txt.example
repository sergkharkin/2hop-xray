# Заграничные ресурсы — дописываются в правило hop-list-foreign.
# Куда ведёт лейн, решает конфиг: в прямом режиме → proxy (выход за границей),
# в обратном (роутер за границей, выход в РФ) → direct.
# Домен на строку, # — комментарий.
linkedin.com
# example.com
