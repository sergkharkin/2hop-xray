# ── Никогда не коммитить реальные конфиги и креды Xray ──────────────────────
# Шаблоны/конфиги с настоящими UUID, Reality-ключами и IP серверов:
config-*.json.bak
*.json.bak
*.json
# исключение: статика веб-интерфейса (там нет секретов)
!hop-ui/**

# Файл авторизации панели (логин:пароль)
hop-auth
disabled

# Ключи и выгрузки кредов
*.key
*-keys*.txt
vless-keys*.txt
example.txt

# Архивы и мусор
*.zip
*.bak.*
.DS_Store
